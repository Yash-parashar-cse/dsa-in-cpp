class Solution {
public:
    int findComplement(int num) {
        int original = num;
        int mask = 0;

        while(num > 0){
            mask = (mask << 1) | 1;
            num = num >> 1;

        }
        return original ^ mask;
    }
};
